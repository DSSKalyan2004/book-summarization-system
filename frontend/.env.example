# Copy this file to .env and fill in your values
# For local development, use .env.development (overrides .env when running `npm run dev`)

# Backend API URL (include /api suffix)
# Local:  /api  (uses Vite proxy)
# Render: https://your-backend-name.onrender.com/api
VITE_API_URL=https://book-summarization-system.onrender.com/api
